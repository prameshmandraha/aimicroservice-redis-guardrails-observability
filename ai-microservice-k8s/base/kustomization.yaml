apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - configmap.yaml
  # - secret.yaml
  - redis-pvc.yaml
  - redis-deployment.yaml
  - redis-service.yaml
  - ai-service-deployment.yaml
  - ai-service-service.yaml
  - hpa.yaml

labels:
  - includeSelectors: false
    pairs:
      app.kubernetes.io/part-of: ai-microservice-stack
